export enum USER_TYPE {
  vendor = "vendor",
  operator = "operator",
}

export enum  User_Address {
  WORK = 'WORK',
  PERMANENT = 'PERMANENT',
  PRESENT = 'PRESENT',
};

export enum  User_Document {
  AADHAR_CARD = 'AADHAR_CARD',
  PAN_CARD = 'PAN_CARD',
};
